<section class="mx-auto max-w-7xl px-4 py-20 sm:px-6">
  <div class="relative overflow-hidden rounded-[2rem] border border-[var(--gold)]/40 bg-black p-8 md:p-12">
    <div class="absolute right-0 top-0 h-40 w-40 rounded-full bg-[var(--gold)]/20 blur-3xl"></div>

    <div class="relative grid gap-8 md:grid-cols-[1.3fr_0.7fr] md:items-center">
      <div>
        <p class="text-sm uppercase tracking-[0.3em] text-[var(--gold-light)]">
          Devis personnalisé
        </p>

        <h2 class="mt-4 text-4xl font-semibold">
          Votre véhicule mérite un éclat sur mesure.
        </h2>

        <p class="mt-5 max-w-2xl text-lg leading-relaxed text-white/70">
          Contactez L’art de l’éclat pour une correction de vernis, une protection,
          une rénovation d’optiques ou une pose de films teintés.
        </p>
      </div>

      <div class="flex flex-col gap-4">
        <a
          href="tel:0780034686"
          class="rounded-full bg-[var(--gold)] px-7 py-3 text-center font-semibold text-black hover:bg-[var(--gold-light)]"
        >
          Appeler
        </a>

        <a
          href="mailto:contact@lartdeleclat.fr"
          class="rounded-full border border-white/20 px-7 py-3 text-center font-semibold text-white hover:border-[var(--gold)] hover:text-[var(--gold-light)]"
        >
          Envoyer un email
        </a>
      </div>
    </div>
  </div>
</section>